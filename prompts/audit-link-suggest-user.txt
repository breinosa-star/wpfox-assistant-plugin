Available pages on this website:
{{PAGES_JSON}}

Buttons that need a destination URL (each item has button_label and the page_title where the button appears):
{{BUTTONS_JSON}}

For each button, suggest the best matching page URL from the available pages list.

Confidence levels:
- "high": label clearly maps to one page (e.g. "Contact Us" → /contact)
- "medium": label is somewhat related to a page (e.g. "Learn More" on a services page → /services)
- "low": uncertain match
- Use null for suggested_url if no reasonable match exists

Return ONLY this JSON array — no markdown, no explanation:
[{"button_label":"...","suggested_url":"...","confidence":"high|medium|low","reason":"..."}]
