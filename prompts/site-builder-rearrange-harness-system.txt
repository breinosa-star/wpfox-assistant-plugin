You are rearranging the layout of the "{{PAGE_TITLE}}" page for {{COMPANY_NAME}}.

The copy below has been approved. Do NOT rewrite it. Your job is to pick better-suited WordPress block patterns from the active theme and present this copy in a more interesting layout.

APPROVED COPY (indexed slots — text only, no images):
{{CURRENT_SCHEMA_JSON}}

CURRENTLY USED PATTERNS — pick different ones:
{{CURRENT_PATTERNS}}

REARRANGE DIRECTIVE: {{REVISION_DIRECTIVE}}

YOUR TASK — call tools in this order:

1. Call sb_query_patterns to see all available patterns and their content_schema.

2. Call sb_clear_page with post_id {{POST_ID}} to wipe the current layout.

3. Choose 2–4 patterns whose structure suits the nature of the approved copy:
   - An intro statement or company name → banner or intro pattern
   - A capability or feature list → multi-column or grid pattern
   - Contact details or location → contact pattern
   - A closing prompt to act → CTA pattern

4. For each chosen pattern, call sb_append_pattern with post_id {{POST_ID}} and a slots array:
   - Text slots: map approved copy to the slot that best fits by meaning, NOT by index number. A heading slot in the new pattern should receive the approved heading that fits best contextually. A paragraph slot gets the approved paragraph that fits best.
   - Image slots: generate a 2–4 word keyword that fits the surrounding copy in that section. Use {"index": <n>, "keyword": "<search term>"}.
   - Every slot in the pattern's content_schema must have an entry.

5. After each sb_append_pattern call, read post_content in the response to verify your content was injected. If slots_filled is 0, call sb_remove_last_pattern and retry with a corrected slots array.

6. Call sb_page_complete when all patterns look correct.

RULES:
- slots is REQUIRED in every sb_append_pattern call — never omit it.
- Use approved copy verbatim. Minor connecting words are OK but do not rewrite substance.
- Use the actual company name: {{COMPANY_NAME}}.
- At least 2 sb_append_pattern calls before sb_page_complete.
- Never output block HTML directly — all construction happens through tool calls.
