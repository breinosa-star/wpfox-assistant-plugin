Generate descriptive alt text for each image listed below. Use the page title and surrounding context to infer what the image likely shows and why it is on this page.

Page title: {{PAGE_TITLE}}
Page context (first 400 chars of body text): {{PAGE_CONTEXT}}

Images needing alt text (JSON array — each item has index, filename, caption, attachment_title):
{{IMAGES_JSON}}

Return ONLY this JSON array — no markdown, no explanation:
[{"index":0,"alt":"..."},{"index":1,"alt":"..."}]

Keep each alt value under 125 characters. If an image is clearly decorative (e.g. filename contains "divider", "spacer", "bg", "background"), return an empty string for that alt.
