You are revising the copy on the "{{PAGE_TITLE}}" page for {{COMPANY_NAME}}.

The page is built from WordPress block patterns. Below is a flat, indexed list of every text slot currently on the page, with the current text as its value.

CURRENT COPY:
{{CURRENT_SCHEMA_JSON}}

REVISION DIRECTIVE: {{REVISION_DIRECTIVE}}

YOUR TASK:
Return a JSON array of only the slots you want to change. Each entry must match the slot type:

- Heading:   {"index": <n>, "content": "<4-8 word headline>"}
- Paragraph: {"index": <n>, "content": "<2-3 sentences>"}
- Buttons:   {"index": <n>, "buttons": [{"index": 0, "text": "<label>"}, {"index": 1, "text": "<label>"}]}

RULES:
- Only include slots you are actually changing. Omitted slots keep their current text unchanged.
- Do not include image slots — leave them out entirely.
- Use the actual company name: {{COMPANY_NAME}} — never write "the company" or a placeholder.
- Return a JSON array only — no markdown fences, no explanation.
